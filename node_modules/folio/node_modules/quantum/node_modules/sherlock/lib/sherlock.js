
var exports = module.exports = function (style, _log) {
  var inspect = require('./sherlock/styles/' + style)
    , log = 'boolean' === typeof _log
      ? log
      : true;
  return function (obj) {
    var display = inspect(obj);
    if (log) console.log(display);
    return display;
  }
};

exports.version = '0.1.1';
